server:
  port: 8000

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/?user=root
    username: root
    password: root
    initialization-mode: always
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    